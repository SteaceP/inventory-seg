import type { Language } from "./user";
import type { TurnstileInstance } from "@marsidev/react-turnstile";

// Auth context types or other shared auth logic can go here in the future.
// Currently, all component-specific props have been moved to their respective components.

export type { Language, TurnstileInstance };
