<div dir="rtl">

<p align="center">
  <a href="README.md">English</a> | 
  <a href="README.fr.md">Français</a> | 
  <b>العربية</b>
</p>

# نظام إدارة المخزون (SEG Inventaire)

تطبيق لإدارة المخزون لشركة La Société Emmanuel-Grégoire، منفصل تماماً عن الإنترانت/الإكسترانت الرئيسي، تم بناؤه باستخدام تقنيات حديثة، ومصمم للكفاءة والوضوح والمزامنة في الوقت الفعلي.

## ✨ المميزات

- **📊 لوحة التحكم**: نظرة عامة شاملة على العناصر، الفئات الرئيسية وتنبيهات انخفاض المخزون.
- **📦 تتبع المخزون**:
  - عرض شبكي مصنف مع أقسام قابلة للطي.
  - مستويات انخفاض المخزون على مستوى العنصر، الفئة، والمستوى العام (**التسلسل الهرمي: العنصر > الفئة > العام**).
  - توليد الرموز السرية (SKU) والباركود ودعم المسح الضوئي.
  - دعم الصور للتتبع البصري.
  - **إدارة المواقع**: تنظيم المخزون بشكل هرمي (مستودع ← رف ← حاوية).
- **🔧 تتبع الأجهزة**:
  - إدارة الأجهزة المنزلية (العلامة التجارية، الموديل، الرقم التسلسلي).
  - تتبع سجل الإصلاحات مع التكاليف ومعلومات مزود الخدمة.
- **📊 التقارير والتحليلات**:
  - تقارير استهلاك المخزون الشهرية والسنوية.
  - تصدير بصيغة PDF/طباعة لعمليات الجرد الفعلي.
- **⚡ تحديثات في الوقت الفعلي**: مزامنة فورية بين الأجهزة عبر Supabase Realtime.
- **🌍 دعم متعدد اللغات**: دعم كامل للغات الإنجليزية والفرنسية والعربية.
- **🎨 التخصيص**:
  - الوضع الداكن والفاتح.
  - عرض مدمج لعرض المعلومات بكثافة عالية.
  - تخصيص ملف تعريف المستخدم (اسم العرض، الصورة الرمزية).
- **🤖 إعادة الطلب التلقائي المدعوم بالذكاء الاصطناعي**:
  - تحليل يومي للمخزون عبر Cloudflare AI (Llama 3).
  - تجميع ذكي حسب المورد (BOD، القرطاسية، إلخ).
  - إشعارات استباقية عندما يكون حجم الطلب مثاليًا.

## 🛠️ التقنيات المستخدمة

- **الواجهة الأمامية**: [React 19](https://react.dev/)، [Vite](https://vitejs.dev/)، [TypeScript](https://www.typescriptlang.org/)
- **واجهة المستخدم والمكونات**: [Material UI (MUI)](https://mui.com/)، [Framer Motion](https://www.framer.com/motion/)
- **قاعدة البيانات والخدمات الخلفية**: [Supabase](https://supabase.com/) (PostgreSQL، Auth، Storage، Realtime)، [Cloudflare Workers AI](https://developers.cloudflare.com/workers-ai/)
- **إدارة الحالة**: React Context API
- **الترجمة**: نظام i18n مخصص

## 🚀 البدء

### المتطلبات الأساسية

- [Node.js](https://nodejs.org/) (الإصدار 24 أو أحدث)
- [pnpm](https://pnpm.io/)
- حساب ومشروع على [Supabase](https://supabase.com/).

### تعليمات التثبيت

1. استنساخ المستودع:

   ```bash
   git clone https://github.com/steace/inventory-seg.git
   cd inventory-seg
   ```

2. تثبيت التبعيات:

   ```bash
   pnpm install
   ```

3. تكوين متغيرات البيئة:

   **هام**: لا تقم أبداً برفع المعلومات السرية إلى مستودع الكود!

   قم بإنشاء ملف `.env.local` في الدليل الرئيسي (يتم تجاهل هذا الملف بواسطة git):

   ```bash
   cp .env.example .env.local
   ```

   ثم قم بتحرير `.env.local` ببيانات Supabase الخاصة بك:

   ```env
   VITE_SUPABASE_URL=https://your-project.supabase.co
   VITE_SUPABASE_PUBLISHABLE_KEY=your_supabase_publishable_key_here
   VITE_VAPID_PUBLIC_KEY=your_vapid_public_key_here
   VITE_VAPID_PRIVATE_KEY=your_vapid_private_key_here
   ```

   **لا تستخدم أبداً** `VITE_SUPABASE_SECRET_KEY` في كود العميل!

4. ترحيل قاعدة البيانات:
   قم بتطبيق الترحيلات الموجودة في مجلد `supabase/migrations` على مشروع Supabase الخاص بك.

5. التشغيل محلياً:

   ```bash
   pnpm run dev
   ```

## 🚀 النشر

### Cloudflare Pages + Workers

هذا التطبيق مصمم ليتم نشره على Cloudflare Pages مع Cloudflare Worker للخدمات الخلفية.

1. **بناء التطبيق:**

   ```bash
   pnpm run build
   ```

2. **تعيين أسرار Cloudflare Worker** (لا تقم برفعها أبداً):

   ```bash
   pnpm dlx wrangler secret put SUPABASE_SECRET_KEY
   pnpm dlx wrangler secret put VAPID_PRIVATE_KEY
   pnpm dlx wrangler secret put BREVO_API_KEY
   ```

3. **نشر الـ Worker:**

   ```bash
   pnpm dlx wrangler deploy
   ```

4. **النشر على Cloudflare Pages:**
   - اربط مستودع GitHub بـ Cloudflare Pages
   - تعيين أمر البناء: `pnpm run build`
   - تعيين دليل المخرجات: `dist`
   - إضافة متغيرات البيئة (متغيرات VITE_* فقط)

### قائمة التحقق من الأمان قبل النشر

- ✅ جميع الأسرار موجودة في `.env.local` (وليس `.env`)
- ✅ ملف `.env.local` موجود في `.gitignore`
- ✅ استخدام `VITE_SUPABASE_PUBLISHABLE_KEY` في العميل (وليس المفتاح السري)
- ✅ تعيين أسرار Cloudflare Worker عبر واجهة سطر الأوامر (CLI)
- ✅ تكوين رؤوس CSP
- ✅ تفعيل سياسات RLS على جميع الجداول في Supabase
- ✅ اختبار عمليات تسجيل الدخول
- ✅ التأكد من عمل الإشعارات (Push Notifications)
- ✅ التحقق من عمل الـ PWA بدون اتصال بالإنترنت

## 🔒 الأمان

يتبع هذا التطبيق أفضل ممارسات الأمان:

- **أمان مستوى الصف (RLS)**: جميع جداول قاعدة البيانات لديها سياسات RLS مفعلة
- **المصادقة**: استخدام Supabase Auth مع إدارة جلسات آمنة
- **التحقق من المدخلات**: تحقق من جانب الخادم في جميع نقاط نهاية الـ API
- **رؤوس CSP**: سياسة أمان المحتوى لمنع هجمات XSS
- **إدارة الأسرار**: لا يتم رفع متغيرات البيئة أبداً إلى الكود
- **منع حقن SQL**: استعلامات معولمة (Parameterized) عبر عميل Supabase
- **HTTPS فقط**: تتطلب الـ PWA اتصال HTTPS للعمليات.

لمشاكل الأمان، راجع [`SECURITY.md`](./SECURITY.md).

## 📜 مخطط قاعدة البيانات

الجداول الرئيسية تشمل:

- `inventory`: يتتبع عناصر المخزون والمستويات.
- `inventory_categories`: يدير المستويات الخاصة بالفئات.
- `inventory_activity`: سجل المراجعة لجميع التغييرات.
- `appliances` & `repairs`: يدير الأجهزة المنزلية وسجل الصيانة.
- `user_settings`: تفضيلات المستخدم وبيانات ملف التعريف.

## 🤝 المساهمة

هذا مشروع شخصي، ولكن الاقتراحات والتحسينات مرحب بها دائماً! لا تتردد في فتح "Issue" أو تقديم "Pull Request".

## 📄 الترخيص

هذا المشروع خاص ومخصص للاستخدام الشخصي. راجع ملف [`LICENSE`](./LICENSE) لمزيد من التفاصيل.

</div>
